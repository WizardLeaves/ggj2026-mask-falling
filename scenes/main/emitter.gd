extends Node2D

var timer: float = 5
var tmp: float = 0
var index: int = 0


"""func _process(delta: float) -> void:
	tmp += delta * timer
	if index * timer < tmp:
		index += 1
		print(index)
		for node in $Type1.get_children():
			node.emit_bullet()
		for node in $Type2.get_children():
			node.emit_bullet()"""
